<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd 
           http://www.springframework.org/schema/context 
           http://www.springframework.org/schema/context/spring-context-3.0.xsd
           http://www.springframework.org/schema/mvc 
           http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd"  default-autowire="byName"
>

	<!-- 静态文件 -->
	<mvc:resources mapping="/operamasks-ui-2.0/**" location="/operamasks-ui-2.0/" />
	<mvc:resources mapping="/script/**" location="/script/" />
	<mvc:resources mapping="/api/**" location="/api/" />
	<mvc:resources mapping="/editor/**" location="/editor/" />
	<mvc:resources mapping="/explorer/**" location="/explorer/" />
	<mvc:resources mapping="/libs/**" location="/libs/" />
	<mvc:resources mapping="/html/**" location="/html/" />
	<mvc:resources mapping="/uploadimage/**" location="/uploadimage/" />
	<mvc:resources mapping="/help/**" location="/help/" />
	<mvc:resources mapping="/card_imgs/**" location="/card_imgs/" />
	
	<!--  在Spring3.0.5这个版本上，<mvc:annotation-driven/>声明是没有 defaultAnnotationHandlerMapping这个属性的
	<mvc:annotation-driven />
	 -->
	 	
	<!-- springMVC拦截器，启用基于注解的处理器映射，添加拦截器，类级别的处理器映射 -->
	<bean id="handlerMapping" class="org.springframework.web.servlet.mvc.annotation.DefaultAnnotationHandlerMapping">
        <property name="interceptors">
            <list>
                <bean class="com.ray.power.base.SpringMVCFliterInterceptor"/>
            </list>
        </property>
	</bean>
	
	<!-- 默认解决springMVC@responsebody的中文乱码问题 -->  
	<bean id="handlerAdapter" class="org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerAdapter" >
		<property name="messageConverters">
	        <list>
	            <bean id="utf8StringHttpMessageConverter" class="com.ray.power.base.UTF8StringHttpMessageConverter" />
	        </list>
	    </property>
	</bean>
	
	<!--
		扫描所有的controller 但是不扫描service 
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service" />
	-->
	<context:component-scan base-package="com.ray.power">
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller" />
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service" />
	</context:component-scan>

	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/jsp/" />
		<property name="suffix" value=".jsp" />
	</bean>
	
	<!-- 上传拦截，如最大上传值及最小上传值  
    <bean id="multipartResolver"  
        class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10000000" />
    </bean> 
 	-->

</beans>